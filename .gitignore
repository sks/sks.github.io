# Jekyll build output
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/

# ── Maintainer-only (keep local + backup/blog — never commit to origin/main) ──
scripts/
.local/
_staging/
AGENTS.md
docs/
